<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.jsm.exptool.ui.camera.CameraViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.camera.view.PreviewView
            android:id="@+id/cameraPreview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true" />

        <FrameLayout
            android:id="@+id/control"
            android:layout_width="match_parent"
            android:layout_height="112dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentBottom="true"
            android:background="@color/white">

            <ImageButton
                android:id="@+id/flashSelector"
                style="@android:style/Widget.Material.Light.Button.Borderless"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|left"
                android:contentDescription="Selección de flash"
                android:onClick="@{(view) -> viewModel.changeFlash(view,context)}"
                android:padding="10dp"
                android:src="@android:drawable/ic_menu_camera" />

            <Button
                android:id="@+id/picture"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:onClick="@{() -> viewModel.takePicture(context)}"
                android:text="Hacer foto" />

            <ImageButton
                android:id="@+id/cameraSelector"
                style="@android:style/Widget.Material.Light.Button.Borderless"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="center_vertical|right"
                android:contentDescription="Selección de cámara"
                android:onClick="@{() -> viewModel.selectCamera(context)}"
                android:padding="10dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_flip_camera" />

        </FrameLayout>

    </RelativeLayout>
</layout>